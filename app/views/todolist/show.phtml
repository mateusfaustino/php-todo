<?php
/**
 * TodoList Show View
 * Display list details with tasks
 */
?>
<section class="container my-5">
    <div class="row">
        <div class="col-12 col-lg-8">
            <div class="card border-0 shadow mb-4">
                <div class="card-header bg-white border-bottom py-3">
                    <div class="d-flex align-items-center justify-content-between">
                        <div class="d-flex align-items-center gap-2">
                            <a href="/lists" class="btn btn-sm btn-outline-secondary">
                                <span class="material-icons">arrow_back</span>
                            </a>
                            <h5 class="mb-0">List Details</h5>
                        </div>
                        <div class="btn-group">
                            <a href="/lists/edit?id=<?= $list->getId() ?>" class="btn btn-sm btn-outline-primary">
                                <span class="material-icons fs-6">edit</span> Edit
                            </a>
                            <?php if ($list->isArchived()): ?>
                                <a href="/lists/unarchive?id=<?= $list->getId() ?>" class="btn btn-sm btn-outline-success">
                                    <span class="material-icons fs-6">unarchive</span> Unarchive
                                </a>
                            <?php else: ?>
                                <a href="/lists/archive?id=<?= $list->getId() ?>" class="btn btn-sm btn-outline-warning">
                                    <span class="material-icons fs-6">archive</span> Archive
                                </a>
                            <?php endif; ?>
                        </div>
                    </div>
                </div>
                <div class="card-body p-4">
                    <div class="d-flex align-items-center gap-3 mb-4">
                        <div class="bg-primary bg-opacity-10 rounded-circle p-3">
                            <span class="material-icons text-primary fs-1">list</span>
                        </div>
                        <div>
                            <h3 class="mb-1"><?= htmlspecialchars($list->getName()) ?></h3>
                            <div class="d-flex align-items-center gap-2">
                                <?php if ($list->isArchived()): ?>
                                    <span class="badge bg-secondary">Archived</span>
                                <?php else: ?>
                                    <span class="badge bg-success">Active</span>
                                <?php endif; ?>
                                <span class="text-muted">Order: <?= $list->getOrder() ?></span>
                            </div>
                        </div>
                    </div>

                    <?php if ($list->getProject()): ?>
                        <div class="mb-3">
                            <h6 class="text-muted mb-2">Project</h6>
                            <p class="mb-0">
                                <span class="material-icons text-muted fs-6">folder</span>
                                <a href="/projects/show?id=<?= $list->getProject()->getId() ?>">
                                    <?= htmlspecialchars($list->getProject()->getName()) ?>
                                </a>
                            </p>
                        </div>
                    <?php endif; ?>
                </div>
            </div>

            <!-- Tasks in this List -->
            <div class="card border-0 shadow">
                <div class="card-header bg-white border-bottom py-3 d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">Tasks (<?= $list->getTasks()->count() ?>)</h5>
                    <a href="/tasks/create?list_id=<?= $list->getId() ?>" class="btn btn-sm btn-primary">
                        <span class="material-icons fs-6">add</span> Add Task
                    </a>
                </div>
                <div class="card-body p-0">
                    <?php if ($list->getTasks()->isEmpty()): ?>
                        <div class="text-center py-5">
                            <span class="material-icons text-muted" style="font-size: 3rem;">task</span>
                            <p class="text-muted mt-2">No tasks in this list yet</p>
                            <a href="/tasks/create?list_id=<?= $list->getId() ?>" class="btn btn-sm btn-primary">Create Task</a>
                        </div>
                    <?php else: ?>
                        <div class="list-group list-group-flush">
                            <?php foreach ($list->getTasks() as $task): ?>
                                <div class="list-group-item p-3">
                                    <div class="d-flex align-items-start gap-3">
                                        <div class="pt-1">
                                            <?php if ($task->isCompleted()): ?>
                                                <span class="material-icons text-success fs-4">check_circle</span>
                                            <?php else: ?>
                                                <a href="/tasks/complete?id=<?= $task->getId() ?>" class="text-decoration-none">
                                                    <span class="material-icons text-muted fs-4">radio_button_unchecked</span>
                                                </a>
                                            <?php endif; ?>
                                        </div>
                                        <div class="flex-grow-1">
                                            <h6 class="mb-1 <?= $task->isCompleted() ? 'text-decoration-line-through text-muted' : '' ?>">
                                                <?= htmlspecialchars($task->getTitle()) ?>
                                            </h6>
                                            <div class="d-flex flex-wrap align-items-center gap-2">
                                                <?php
                                                $priorityColors = [
                                                    'LOW' => 'bg-success',
                                                    'MEDIUM' => 'bg-info',
                                                    'HIGH' => 'bg-warning',
                                                    'URGENT' => 'bg-danger'
                                                ];
                                                $priorityColor = $priorityColors[$task->getPriority()->value] ?? 'bg-secondary';
                                                ?>
                                                <span class="badge <?= $priorityColor ?>"><?= $task->getPriority()->value ?></span>
                                                <?php if ($task->getDueDate()): ?>
                                                    <small class="text-muted">
                                                        <span class="material-icons fs-6">event</span>
                                                        <?= $task->getDueDate()->format('M d, Y') ?>
                                                    </small>
                                                <?php endif; ?>
                                            </div>
                                        </div>
                                        <div class="btn-group">
                                            <a href="/tasks/show?id=<?= $task->getId() ?>" class="btn btn-sm btn-outline-primary">
                                                <span class="material-icons fs-6">visibility</span>
                                            </a>
                                            <a href="/tasks/edit?id=<?= $task->getId() ?>" class="btn btn-sm btn-outline-secondary">
                                                <span class="material-icons fs-6">edit</span>
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>

        <div class="col-12 col-lg-4">
            <div class="card border-0 shadow">
                <div class="card-header bg-white border-bottom py-3">
                    <h6 class="mb-0">Actions</h6>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a href="/tasks/create?list_id=<?= $list->getId() ?>" class="btn btn-outline-primary">
                            <span class="material-icons">add</span> Add Task
                        </a>
                        <a href="/lists/edit?id=<?= $list->getId() ?>" class="btn btn-outline-secondary">
                            <span class="material-icons">edit</span> Edit List
                        </a>
                        <a href="/lists/delete?id=<?= $list->getId() ?>" class="btn btn-outline-danger"
                           onclick="return confirm('Are you sure you want to delete this list?')">
                            <span class="material-icons">delete</span> Delete List
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
