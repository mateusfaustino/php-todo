<?php
/**
 * Project Show View
 * Display project details
 */
?>
<section class="container my-5">
    <div class="row">
        <div class="col-12 col-lg-8">
            <div class="card border-0 shadow mb-4">
                <div class="card-header bg-white border-bottom py-3">
                    <div class="d-flex align-items-center justify-content-between">
                        <div class="d-flex align-items-center gap-2">
                            <a href="/projects" class="btn btn-sm btn-outline-secondary">
                                <span class="material-icons">arrow_back</span>
                            </a>
                            <h5 class="mb-0">Project Details</h5>
                        </div>
                        <div class="btn-group">
                            <a href="/projects/edit?id=<?= $project->getId() ?>" class="btn btn-sm btn-outline-primary">
                                <span class="material-icons fs-6">edit</span> Edit
                            </a>
                            <?php if ($project->isArchived()): ?>
                                <a href="/projects/unarchive?id=<?= $project->getId() ?>" class="btn btn-sm btn-outline-success">
                                    <span class="material-icons fs-6">unarchive</span> Unarchive
                                </a>
                            <?php else: ?>
                                <a href="/projects/archive?id=<?= $project->getId() ?>" class="btn btn-sm btn-outline-warning">
                                    <span class="material-icons fs-6">archive</span> Archive
                                </a>
                            <?php endif; ?>
                        </div>
                    </div>
                </div>
                <div class="card-body p-4">
                    <div class="d-flex align-items-center gap-3 mb-4">
                        <div class="rounded-circle d-flex align-items-center justify-content-center"
                             style="width: 64px; height: 64px; background-color: <?= htmlspecialchars($project->getColor()) ?>20;">
                            <span class="material-icons fs-1" style="color: <?= htmlspecialchars($project->getColor()) ?>">folder</span>
                        </div>
                        <div>
                            <h3 class="mb-1"><?= htmlspecialchars($project->getName()) ?></h3>
                            <div class="d-flex align-items-center gap-2">
                                <?php if ($project->isArchived()): ?>
                                    <span class="badge bg-secondary">Archived</span>
                                <?php else: ?>
                                    <span class="badge bg-success">Active</span>
                                <?php endif; ?>
                                <span class="text-muted">Created on <?= $project->getCreatedAt()->format('F d, Y') ?></span>
                            </div>
                        </div>
                    </div>

                    <?php if ($project->getDescription()): ?>
                        <div class="mb-4">
                            <h6 class="text-muted mb-2">Description</h6>
                            <p class="mb-0"><?= nl2br(htmlspecialchars($project->getDescription())) ?></p>
                        </div>
                    <?php endif; ?>

                    <div class="row">
                        <div class="col-md-6">
                            <h6 class="text-muted mb-2">Color</h6>
                            <div class="d-flex align-items-center gap-2">
                                <span class="badge" style="background-color: <?= htmlspecialchars($project->getColor()) ?>; color: white;">
                                    <?= htmlspecialchars($project->getColor()) ?>
                                </span>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <h6 class="text-muted mb-2">Owner</h6>
                            <p class="mb-0">
                                <span class="material-icons text-muted fs-6">person</span>
                                <?= htmlspecialchars($project->getUser()->getName()) ?>
                            </p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Lists in this Project -->
            <div class="card border-0 shadow">
                <div class="card-header bg-white border-bottom py-3">
                    <h5 class="mb-0">Lists in this Project</h5>
                </div>
                <div class="card-body">
                    <?php if ($project->getLists()->isEmpty()): ?>
                        <div class="text-center py-4">
                            <span class="material-icons text-muted" style="font-size: 3rem;">list</span>
                            <p class="text-muted mt-2">No lists in this project yet</p>
                            <a href="/lists/create" class="btn btn-sm btn-primary">Create List</a>
                        </div>
                    <?php else: ?>
                        <div class="list-group list-group-flush">
                            <?php foreach ($project->getLists() as $list): ?>
                                <a href="/lists/show?id=<?= $list->getId() ?>" class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                                    <div>
                                        <h6 class="mb-1"><?= htmlspecialchars($list->getName()) ?></h6>
                                        <small class="text-muted"><?= $list->getTasks()->count() ?> tasks</small>
                                    </div>
                                    <span class="material-icons text-muted">chevron_right</span>
                                </a>
                            <?php endforeach; ?>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>

        <div class="col-12 col-lg-4">
            <div class="card border-0 shadow">
                <div class="card-header bg-white border-bottom py-3">
                    <h6 class="mb-0">Actions</h6>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a href="/lists/create" class="btn btn-outline-primary">
                            <span class="material-icons">add</span> Add List
                        </a>
                        <a href="/projects/edit?id=<?= $project->getId() ?>" class="btn btn-outline-secondary">
                            <span class="material-icons">edit</span> Edit Project
                        </a>
                        <a href="/projects/delete?id=<?= $project->getId() ?>" class="btn btn-outline-danger"
                           onclick="return confirm('Are you sure you want to delete this project?')">
                            <span class="material-icons">delete</span> Delete Project
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
