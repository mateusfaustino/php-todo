<?php
/**
 * Project Edit View
 * Form to edit an existing project
 */
?>
<section class="container my-5">
    <div class="row justify-content-center">
        <div class="col-12 col-lg-8">
            <div class="card border-0 shadow">
                <div class="card-header bg-white border-bottom py-3">
                    <div class="d-flex align-items-center gap-2">
                        <a href="/projects" class="btn btn-sm btn-outline-secondary">
                            <span class="material-icons">arrow_back</span>
                        </a>
                        <h5 class="mb-0">Edit Project</h5>
                    </div>
                </div>
                <div class="card-body p-4">
                    <?php if (isset($error)): ?>
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <span class="material-icons">error</span>
                            <?= htmlspecialchars($error) ?>
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    <?php endif; ?>

                    <form action="/projects/update" method="POST">
                        <input type="hidden" name="id" value="<?= $project->getId() ?>">

                        <div class="mb-3">
                            <label for="name" class="form-label">Project Name <span class="text-danger">*</span></label>
                            <input type="text" class="form-control" id="name" name="name" required
                                   value="<?= htmlspecialchars($project->getName()) ?>" maxlength="100">
                        </div>

                        <div class="mb-3">
                            <label for="description" class="form-label">Description</label>
                            <textarea class="form-control" id="description" name="description" rows="3"><?= htmlspecialchars($project->getDescription() ?? '') ?></textarea>
                        </div>

                        <div class="mb-3">
                            <label for="color" class="form-label">Color</label>
                            <div class="d-flex align-items-center gap-2">
                                <input type="color" class="form-control form-control-color" id="color" name="color"
                                       value="<?= htmlspecialchars($project->getColor()) ?>" title="Choose project color">
                                <span class="text-muted">Choose a color for the project</span>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Owner</label>
                            <input type="text" class="form-control" value="<?= htmlspecialchars($project->getUser()->getName()) ?>" disabled>
                            <small class="text-muted">Project owner cannot be changed</small>
                        </div>

                        <div class="mb-4">
                            <label class="form-label">Status</label>
                            <div>
                                <?php if ($project->isArchived()): ?>
                                    <span class="badge bg-secondary">Archived</span>
                                    <a href="/projects/unarchive?id=<?= $project->getId() ?>" class="btn btn-sm btn-outline-success ms-2">
                                        <span class="material-icons fs-6">unarchive</span> Unarchive
                                    </a>
                                <?php else: ?>
                                    <span class="badge bg-success">Active</span>
                                    <a href="/projects/archive?id=<?= $project->getId() ?>" class="btn btn-sm btn-outline-warning ms-2">
                                        <span class="material-icons fs-6">archive</span> Archive
                                    </a>
                                <?php endif; ?>
                            </div>
                        </div>

                        <div class="d-flex gap-2 justify-content-end">
                            <a href="/projects" class="btn btn-outline-secondary">Cancel</a>
                            <button type="submit" class="btn btn-primary">
                                <span class="material-icons">save</span>
                                Update Project
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>
