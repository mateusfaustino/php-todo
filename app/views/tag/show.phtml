<?php
/**
 * Tag Show View
 * Display tag details with associated tasks
 */
?>
<section class="container my-5">
    <div class="row">
        <div class="col-12 col-lg-8">
            <div class="card border-0 shadow mb-4">
                <div class="card-header bg-white border-bottom py-3">
                    <div class="d-flex align-items-center justify-content-between">
                        <div class="d-flex align-items-center gap-2">
                            <a href="/tags" class="btn btn-sm btn-outline-secondary">
                                <span class="material-icons">arrow_back</span>
                            </a>
                            <h5 class="mb-0">Tag Details</h5>
                        </div>
                        <div class="btn-group">
                            <a href="/tags/edit?id=<?= $tag->getId() ?>" class="btn btn-sm btn-outline-primary">
                                <span class="material-icons fs-6">edit</span> Edit
                            </a>
                        </div>
                    </div>
                </div>
                <div class="card-body p-4">
                    <div class="d-flex align-items-center gap-3 mb-4">
                        <span class="material-icons fs-1" style="color: <?= htmlspecialchars($tag->getColor()) ?>;">label</span>
                        <div>
                            <h3 class="mb-1"><?= htmlspecialchars($tag->getName()) ?></h3>
                            <span class="badge fs-6" style="background-color: <?= htmlspecialchars($tag->getColor()) ?>;">
                                <?= htmlspecialchars($tag->getName()) ?>
                            </span>
                        </div>
                    </div>

                    <div class="mb-3">
                        <h6 class="text-muted mb-2">Color</h6>
                        <div class="d-flex align-items-center gap-2">
                            <div style="width: 30px; height: 30px; background-color: <?= htmlspecialchars($tag->getColor()) ?>; border-radius: 4px;"></div>
                            <code><?= htmlspecialchars($tag->getColor()) ?></code>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Tasks with this Tag -->
            <div class="card border-0 shadow">
                <div class="card-header bg-white border-bottom py-3">
                    <h5 class="mb-0">Tasks with this Tag (<?= $tag->getTasks()->count() ?>)</h5>
                </div>
                <div class="card-body p-0">
                    <?php if ($tag->getTasks()->isEmpty()): ?>
                        <div class="text-center py-5">
                            <span class="material-icons text-muted" style="font-size: 3rem;">task</span>
                            <p class="text-muted mt-2">No tasks with this tag yet</p>
                            <a href="/tasks/create" class="btn btn-sm btn-primary">Create Task</a>
                        </div>
                    <?php else: ?>
                        <div class="list-group list-group-flush">
                            <?php foreach ($tag->getTasks() as $task): ?>
                                <div class="list-group-item p-3">
                                    <div class="d-flex align-items-start gap-3">
                                        <div class="pt-1">
                                            <?php if ($task->isCompleted()): ?>
                                                <span class="material-icons text-success fs-4">check_circle</span>
                                            <?php else: ?>
                                                <span class="material-icons text-muted fs-4">radio_button_unchecked</span>
                                            <?php endif; ?>
                                        </div>
                                        <div class="flex-grow-1">
                                            <h6 class="mb-1 <?= $task->isCompleted() ? 'text-decoration-line-through text-muted' : '' ?>">
                                                <?= htmlspecialchars($task->getTitle()) ?>
                                            </h6>
                                            <div class="d-flex flex-wrap align-items-center gap-2">
                                                <?php
                                                $priorityColors = [
                                                    'LOW' => 'bg-success',
                                                    'MEDIUM' => 'bg-info',
                                                    'HIGH' => 'bg-warning',
                                                    'URGENT' => 'bg-danger'
                                                ];
                                                $priorityColor = $priorityColors[$task->getPriority()->value] ?? 'bg-secondary';
                                                ?>
                                                <span class="badge <?= $priorityColor ?>"><?= $task->getPriority()->value ?></span>
                                                <?php if ($task->getDueDate()): ?>
                                                    <small class="text-muted">
                                                        <span class="material-icons fs-6">event</span>
                                                        <?= $task->getDueDate()->format('M d, Y') ?>
                                                    </small>
                                                <?php endif; ?>
                                                <small class="text-muted">
                                                    <span class="material-icons fs-6">list</span>
                                                    <?= htmlspecialchars($task->getList()->getName()) ?>
                                                </small>
                                            </div>
                                        </div>
                                        <div class="btn-group">
                                            <a href="/tasks/show?id=<?= $task->getId() ?>" class="btn btn-sm btn-outline-primary">
                                                <span class="material-icons fs-6">visibility</span>
                                            </a>
                                            <a href="/tasks/edit?id=<?= $task->getId() ?>" class="btn btn-sm btn-outline-secondary">
                                                <span class="material-icons fs-6">edit</span>
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>

        <div class="col-12 col-lg-4">
            <div class="card border-0 shadow">
                <div class="card-header bg-white border-bottom py-3">
                    <h6 class="mb-0">Actions</h6>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a href="/tags/edit?id=<?= $tag->getId() ?>" class="btn btn-outline-secondary">
                            <span class="material-icons">edit</span> Edit Tag
                        </a>
                        <a href="/tags/delete?id=<?= $tag->getId() ?>" class="btn btn-outline-danger"
                           onclick="return confirm('Are you sure you want to delete this tag?')">
                            <span class="material-icons">delete</span> Delete Tag
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
