<?php
/**
 * Tag List View
 * Displays all tags with filtering and actions
 */
?>
<section class="container my-5">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center mb-4 gap-3">
        <div>
            <h1 class="d-flex align-items-center gap-2 mb-1">
                <span class="material-icons fs-1">label</span>
                Tags
            </h1>
            <p class="text-muted mb-0">Organize and categorize your tasks with tags</p>
        </div>
        <a href="/tags/create" class="btn btn-primary d-flex align-items-center gap-2">
            <span class="material-icons">add</span>
            New Tag
        </a>
    </div>

    <!-- Stats Card -->
    <div class="row g-3 mb-4">
        <div class="col-12 col-md-6">
            <div class="card border-0 shadow-sm bg-primary bg-opacity-10">
                <div class="card-body d-flex align-items-center gap-3">
                    <div class="bg-primary bg-opacity-25 rounded-circle p-3">
                        <span class="material-icons text-primary fs-3">label</span>
                    </div>
                    <div>
                        <h6 class="text-muted mb-1">Total Tags</h6>
                        <h3 class="mb-0"><?= $totalCount ?? 0 ?></h3>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-12 col-md-6">
            <div class="card border-0 shadow-sm bg-info bg-opacity-10">
                <div class="card-body d-flex align-items-center gap-3">
                    <div class="bg-info bg-opacity-25 rounded-circle p-3">
                        <span class="material-icons text-info fs-3">task</span>
                    </div>
                    <div>
                        <h6 class="text-muted mb-1">Tagged Tasks</h6>
                        <h3 class="mb-0"><?= $taggedTasks ?? 0 ?></h3>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Tags Grid -->
    <div class="row g-4">
        <?php if (empty($tags)): ?>
            <div class="col-12">
                <div class="card border-0 shadow text-center py-5">
                    <div class="card-body">
                        <span class="material-icons text-muted" style="font-size: 4rem;">label_off</span>
                        <h5 class="mt-3 text-muted">No tags found</h5>
                        <p class="text-muted">Create your first tag to categorize tasks</p>
                        <a href="/tags/create" class="btn btn-primary">Create Tag</a>
                    </div>
                </div>
            </div>
        <?php else: ?>
            <?php foreach ($tags as $tag): ?>
                <div class="col-6 col-md-4 col-lg-3">
                    <div class="card border-0 shadow h-100">
                        <div class="card-body text-center">
                            <span class="material-icons fs-1 mb-2" style="color: <?= htmlspecialchars($tag->getColor()) ?>;">label</span>
                            <h6 class="card-title mb-1"><?= htmlspecialchars($tag->getName()) ?></h6>
                            <p class="text-muted small mb-2"><?= $tag->getTasks()->count() ?> tasks</p>
                            <div class="btn-group btn-group-sm">
                                <a href="/tags/show?id=<?= $tag->getId() ?>" class="btn btn-outline-primary" title="View">
                                    <span class="material-icons fs-6">visibility</span>
                                </a>
                                <a href="/tags/edit?id=<?= $tag->getId() ?>" class="btn btn-outline-secondary" title="Edit">
                                    <span class="material-icons fs-6">edit</span>
                                </a>
                                <a href="/tags/delete?id=<?= $tag->getId() ?>" class="btn btn-outline-danger" title="Delete" onclick="return confirm('Are you sure?')">
                                    <span class="material-icons fs-6">delete</span>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        <?php endif; ?>
    </div>
</section>
